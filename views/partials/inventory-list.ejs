<ul class="inventory-list">
    <% for(const[type, ore] of Object.entries(inventory)) { %>
        <li class="inventory-list-item"><%= ore.amount + "x " + ore.name + " " + ore.totalPrice %><button>Sell</button></li>
    <% } %>
</ul>